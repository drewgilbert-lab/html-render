/**
 * A stacked set of named methodology limitations, each with a melon left-border accent.
 */
export interface LimitationsCardsProps {
  items?: { title: React.ReactNode; body: React.ReactNode }[];
}
export function LimitationsCards(props: LimitationsCardsProps): JSX.Element;
