One self-contained aside inside body copy.

```jsx
<Callout label="Why It Matters">Always check the freshness date before acting on a ranking.</Callout>
<Callout label="Watch Out" variant="melon">Install share is not revenue share.</Callout>
```

Two variants only — don't invent others. For three or more named limitations use `LimitationsCards`.
